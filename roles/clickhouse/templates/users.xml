<clickhouse>
    <users>
{% for name, user in clickhouse.users | dictsort %}
        <{{ name }}>
            <password_sha256_hex>{{ user.password | hash('sha256') }}</password_sha256_hex>
            <profile>default</profile>
            <quota>default</quota>
            <networks>
                <ip>::/0</ip>
            </networks>
            <grants>
{% for grant in user.grants | default([]) %}
                <query>{{ grant | e }}</query>
{% endfor %}
            </grants>
        </{{ name }}>
{% endfor %}
    </users>
</clickhouse>
