---
platforms:
  - name: debian-13
    image: dokken/debian-13
    pre_build_image: true
    command: /lib/systemd/systemd
    privileged: true
    cgroup_parent: docker.slice

provisioner:
  inventory:
    group_vars:
      all:
        # `bootstrap` is a plain dict override — it replaces the role defaults
        # wholesale, so every key the role reads has to be listed here.
        bootstrap:
          # the three flags below would reach the host kernel through the
          # privileged container, so they stay off and are verified as gated
          disable_algif_aead: false
          disable_transparent_huge_pages: false
          set_hostname: true
          setup_users: true
          setup_sudo: true
          limits:
            enabled: true
            nofile:
              soft: 8192
              hard: 1048576
          swap:
            enabled: false
            # NOT /swapfile: /proc/swaps is not namespaced, so `swapon --show`
            # in the container lists the docker *host's* active swap, and GitHub
            # runners swap on a file at /swapfile. teardown-swap.yaml would then
            # match that entry and swapoff a file this container never activated.
            # The name also becomes a systemd unit ({{ file[1:] }}.swap), so it
            # must contain no dash - systemd escapes those to \x2d.
            file: /molecule_swapfile
            size_mb: 4096
        dir:
          data: /data
          ansible: /data/ansible
        # public key comes from molecule/ssh_keys/<user>.pub, which is where
        # the role looks it up ({{ playbook_dir }}/ssh_keys/)
        users:
          moleculetest:
            groups: sudo
            create_home: true
