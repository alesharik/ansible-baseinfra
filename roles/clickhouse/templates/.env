{% for u in lookup('ansible.builtin.dict', clickhouse.users) %}
{{ u.key }}_PASSWORD={{ u.value.password }}
{% endfor %}
CLICKHOUSE_PASSWORD={{ clickhouse.root.password }}
CLICKHOUSE_USER={{ clickhouse.root.user }}